node_modules/
dist/
*.db
*.db-journal
*.db-wal
.DS_Store
web/node_modules/
__pycache__/

# Secrets
.env
.env.local

# Backup files
*.bak.*

# Volumes / local data
data/
cache/
tsconfig.tsbuildinfo

# Coverage reports
coverage/

# Claude Code agent worktrees (local only)
.claude/worktrees/

# Battle-test runtime artifacts (DBs, vault scratch) — keep plans + harnesses, drop data
.battle/artifacts/
# Web-dashboard verification screenshots (regenerable by scripts/battle/verify-web.mjs)
scripts/battle/web-shots/

# battle-v7 workflow scratch (scripts + preserved artifacts) + skeptic worktrees
.battle/
.battle-skeptic*/

# Internal process artifacts — never commit
.planning/
.battle/
.claude/commands/
