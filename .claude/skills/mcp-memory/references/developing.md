# Developing the server (contributor reference)

For changing the server itself. Pair with `architecture.md` (map) and `/.planning/CODEBASE-MAP.md` (exhaustive).

## Contents
1. Build / test / verify commands
2. Testing conventions
3. Roadmap — shipped vs remaining
4. Current open items (honest)
5. Competitive positioning
6. Doc drift to fix

---

## 1. Build / test / verify commands

```bash
npm run build      # tsc
npm test           # vitest — 1000 tests (mock embedder, :memory: SQLite, deterministic)
npm run smoke      # REAL MCP stdio JSON-RPC round-trip (49 tools, real models)
npm run bench      # REAL retrieval quality + latency (gold set, real embedder + reranker)

# Real-runtime simulations (real model + real SQLite + real git):
node scripts/battle/sim-solo.mjs        # RERANK=1 for the rerank path; P@1/MRR
node scripts/battle/sim-team.mjs        # 2-dev git vault: recall, lossless, union merge
node scripts/battle/verify-nli.mjs      # real NLI write-gate (DeBERTa-v3-xsmall)
node scripts/battle/verify-hooks.mjs    # all 4 hooks live
node scripts/battle/verify-scale.mjs    # 1K/10K/50K latency
node scripts/battle/verify-web.mjs      # boot server + drive dashboard headless (needs `npm run build:web` first)
node scripts/battle/verify-stress.mjs   # concurrency: in-proc burst + multi-PROCESS WAL contention + crash-restart durability
node scripts/battle/verify-load.mjs     # boots the REAL authed server: bearer auth matrix + rate-limit 429/Retry-After + 300-way concurrent load + 400/413 envelopes
node scripts/battle/verify-ui-e2e.mjs   # Playwright E2E: the dashboard Tools console drives read/write/search/destructive over authed /mcp with real persisted effects (needs build:web + global playwright)
```
Models cache under `node_modules/@huggingface/transformers/.cache` (embedder, reranker, NLI all present offline). Worktree agents: `ln -s <repo>/node_modules node_modules` before running.

**Gates:** `npm run build` clean + `npm test` green are mandatory. For any quality/behavior claim, run the relevant real sim — mock tests alone don't prove retrieval quality or the real-model paths.

## 2. Testing conventions

- Two Vitest projects: backend (`vitest.config.ts`, node, `src/**/*.test.ts`) and web (`web/vitest.config.ts`, jsdom). Backend thresholds enforced: **lines/statements 100, functions 99, branches 90** (with a documented exclude list).
- Shared harnesses: `createTestDb()` (fresh `:memory:`, full schema + migrations re-run from 0) and `MockEmbeddingProvider` (deterministic 384-dim hashed vectors, no download). API tests boot the real Express app via `buildApp` on an ephemeral port with raw `node:http`. Rerankers/NLI use injected stubs.
- **TDD throughout** — red → green, full suite is the gate. Every test file opens with a JSDoc tying it to a phase/pillar/bug-ID.
- **Untested surfaces to be careful around:** the MCP dispatch path (`createServer`/`instrument`/41-tool registration/`memory_delete` `innerType()` reconstruction) is exercised only by `smoke`, not unit tests — a dropped/renamed tool passes CI. The `extract-entities` handler, `graphCache` namespace isolation, and the search-log writer→reader contract are thin/incidental.

## 3. Roadmap — shipped vs remaining

The R0–R8 "revolutionary roadmap" is **partially shipped** — not a clean BUILT/UNBUILT split. Current truth:

| R | Feature | Status |
|---|---|---|
| R0 | Reproducible local benchmark harness | ✅ Shipped (`npm run bench`, `docs/BENCHMARKS.md`) |
| R1 | Bi-temporal memory | ◑ **Validity shipped** (valid_from/valid_to/tx_expired, invalidate-don't-delete, `as_of` validity queries, `memory_history`). **Remaining:** `as_of` *content* reconstruction (returns current content for historical instants). |
| R2 | Self-weaving graph + Personalized PageRank | ✅ Shipped (IDF-weighted edges, wikilinks→typed edges, PageRank fused into hybrid) |
| R3 | Self-correcting NLI write-gate | ✅ Shipped (runs on **every** store on the near-dup shortlist; real DeBERTa verified; contradiction → bi-temporal invalidate) |
| R4 | Local cross-encoder reranker | ✅ Shipped (ms-marco-MiniLM-L-6-v2; **default ON at MCP** over top-50) |
| R5 | Two-way Obsidian + typed Canvas | ◑ **content_hash change-gate shipped** (M6.1 — git-checkout no longer re-embeds the vault) + change-propagation over REAL `derived_from` edges (M3.3, NOT the `depends_on`/`supersedes` edges prod never writes). **Remaining:** live fs watcher (deferred — no chokidar dep). |
| R6 | Pinned core-memory tier | ✅ **Shipped** (`core_memory_*`, `memory_tiers`) + **auto-load at SessionStart** (M5.3 — real `(scope,namespace)`, newlines preserved). |
| R7 | GDPR-grade compliance layer | ◑ **Forget shipped** (`memory_forget` soft/hard + export-on-hard). **Remaining:** consent tags, retention windows, verifiable RTBF, exportable access trail. |
| R8 | Pluggable embeddings + adaptive router | ◑ **Shipped (opt-in layer, M6.4):** `EmbeddingRegistry` + privacy-gated `selectEmbedder` (confidential/restricted → forced local) + Matryoshka `truncateTo` (preserves the single-fixed-dim `memories_vec` invariant) + `OllamaEmbeddingProvider`; v11 `embedding_model`/`embedding_dim` columns record per-row model. **Remaining:** auto-wire `selectEmbedder` into the singleton hot path (per-call access-level threading). |

**M0–M6 milestones (post-engine, on local `main`):** M0 (registerTool migration + annotations + `instructions`, README/docs truth), M1.2 (ingest_source_tracking wired), **M2 (trust/provenance moat — signed envelope + `memory_verify`, redaction gate, groundedness, vault egress + integrity manifest; twice battle-hardened)**, **M3 (event bus + SSRF guard + change-propagation + `memory_insights`/`memory_health`/`memory_revalidate`/`memory_webhook`)**, **M4 (anchor entities work_item/pull_request/commit + turn-signal gate)**, **M5 (`memory_session_state`, `memory_expertise`, core-memory auto-load)**, **M6 (vault content-hash gate, `compute-governor`, `memory_export_dataset`, pluggable-embeddings layer)**. Schema is **v11** (webhook_targets/deliveries + memories.revalidation_status/embedding_model/embedding_dim). M2-LOW closed: redaction whitespace-split bypass + multi-machine trust store (`MCP_TRUSTED_PUBKEYS`). Each milestone TDD'd + integration-battled (M2 found 20, M3 found 14 — all fixed/regression-locked).

## 4. Current open items (honest)

Two fix waves are CLOSED — **do not treat them as open**:
- The original **9-bug battle queue** (embedding cache key collision, getReadOnlyDb migrate, export resurrects dead rows, stats/manifest dead rows, update raw re-embed, NLI-only-on-supersede, vec0 survives invalidation, consolidate `results_count`, extract-dedup threshold), retired into single-source modules.
- The **battle-overhaul pass** (8-persona hands-on testing → 5 fixes, TDD, 1000 tests green): (1) `expires_at` TTL ISO-Z collation across search/stats/consolidate/expired-delete/session-start + `condensed_at`/`superseded_at` writes — expired rows had leaked into search; (2) `vault_status` raw-mtime compare (was "changed" forever on sub-ms FS); (3) `importance_score` settable on store/update; (4) `memory_graph` false strength comment + `idf_strength` surfaced; (5) vault round-trip recovers `importance_score`/`created_at`/`updated_at` from frontmatter.

Two more fixes landed after the overhaul pass:
- **fix(bitemporal): `as_of` VECTOR reconstruction of retired facts** — `invalidateMemory` now RETAINS the vec row (only hard delete drops it), `superseded_at IS NULL` is scoped to current mode, and `handleRelated`/`detectConflicts` filter `valid_to`/`tx_expired`. Real-model verified (a semantic query reconstructs the retired fact at `as_of`, excludes it from current search, returns the live fact). 1001 tests green.
- **feat(forget): soft-forget un-tombstone via `memory_restore`** — `reinstateMemory` (inverse of `invalidateMemory`) clears `valid_to`/`tx_expired`; `handleRestore` extended to reinstate a soft-forgotten memory into default recall (and mirror the vault file back) in addition to un-condensing — both applied when both hold. Real-model verified (store→soft-forget→excluded→restore→reincluded, stamps cleared). 1005 tests green.
- **feat(graph): alias expansion in the entity-seed paths** — centralized alias resolution in `entity-store.ts` (`resolveToCanonicalName` + `entityIdsByNameOrAlias`, one source); `linkQueryEntities` (the `use_graph` / PageRank seed) now resolves a query's alias → canonical entity id, and `graph.ts` was refactored onto the shared resolver (DRY, retiring its inlined copy). Real-model verified (bare-alias `PG` search with `use_graph` surfaces the PostgreSQL memory at rank 0). 1008 tests green. **`memory_query` left as-is** (content-seeds; `use_graph` off by design).
- **fix(test): de-flake `forgetting.test.ts`** — `applyTemporalDecay` reads `new Date()` on every call; the `"leaves the other decay types unchanged"` test compared two separate `exponential` calls with strict `.toBe()`, so when the two adjacent reads straddled a millisecond boundary (more likely under parallel CPU load) the age shifted ~1e-8 days and the result in its ~12th digit, failing equality (passed isolated/fast). Root cause proven by forcing a 1ms boundary (`a===b` false, `|a−b|`=2.1e-10). Fixed by pinning the clock (`vi.useFakeTimers` + `setSystemTime`) for the whole clock-dependent describe so every read returns the same instant — the assertions test the decay invariants, not clock jitter. Production decay code unchanged (wall-clock read is correct). 0/20 isolated + 1008 green.

A **14-persona battle wave** (8 refreshed + 6 new: concurrency/WAL, REST-tenancy, migration, i18n, real-model-quality, dashboard) with an adversarial refute-verify pass produced **5 more fixes** (TDD, 1014 tests green, build + smoke clean, the 2 majors real-runtime verified):
- **fix(sanitize): preserve U+200C/U+200D** — the output chokepoint's Trojan-Source range swallowed ZWNJ/ZWJ, corrupting all ZWJ emoji + Persian/Arabic/Hindi in *every* result; narrowed `ZERO_WIDTH_BIDI_RE` to keep the joiners (defense unchanged). **[major, P12]**
- **fix(vault): recover `agent_id` + `access_level` on `vault_sync`** — the writer emitted both and the rebuild path recovered them, but `vault_sync` hardcoded `access_level='internal'` and dropped `agent_id`, breaking `memory_attribution.by_agent` on a round-trip. **[major, P3]**
- **fix(stats): `total_memories` excludes expired** (`liveConditions({excludeExpired:true})`) so stats agrees with search; separate `expired_count` keeps a base set. **[P7]**
- **fix(search): `hybridSearch` defaults `offset` to 0** — omitted offset was `slice(undefined,NaN)`→`[]` with `total>0`. **[P5]**
- **fix(import): preserve explicit `importance_score`** (schema + handler) so backup/restore isn't lossy. **[P6/P13]**
- One reported "major" (default `on_conflict:'add'` folds a distinct fact as NOOP) was **refuted** by the adversarial verifier — did not reproduce. No failed capabilities; production untouched (all personas used `/tmp` throwaway DBs).

An **open-items fix wave** (the documented robustness/security backlog, 5 fixes via worktree-isolated TDD agents + adversarial review, 1040 tests green, build + smoke clean) closed the write-isolation, migration, teardown, and logger gaps:
- **fix(server): force-scope `memory_store`** (F1) — the read tools wrapped input in `withForcedNs` (= `scopeToNamespace`) but `memory_store` passed bare `parsed`, so a namespace-forced deployment could WRITE cross-namespace (read isolation held, write leaked). One-line wrap mirrors the reads. REST has no create route (PATCH carries no namespace; PATCH/DELETE guard via `idIsInForcedNamespace`).
- **fix(server): force-scope the remaining ns-bearing tools** (F1b, from F1's adversarial review) — `memory_ingest`, `memory_session_note`, `core_memory_get/append/replace` also passed bare `parsed`; all carry top-level namespace → same wrap. An `it.each` wiring guard over all 6 registrations is the regression tripwire (verified RED on each unwrapped handler).
- **fix(migrations): throw on corrupt `schema_version`** (P11) — `parseInt('garbage')`→NaN made every `m.version > NaN` false → `pending=[]` → silently applied ZERO migrations against an un-migrated schema. The guard validates the RAW string with **`/^\d+$/` (canonical decimal)** — NOT numeric coercion. (First cut used `Number()`+`Number.isInteger`, which battle-v3 P7/P11 proved still let `'0x9'`→9, `'1e1'`→10, `''`→0, `'1.0'`→1 masquerade as a version; hardened to the regex.) Benign paths (missing→0, `'0'` seed, valid decimals) unchanged. Truly fail-closed for the schema_version string.
- **fix(connection): close `createDatabase` survivors at exit** (P10/P14) — uncached SQLite connections were untracked, so a sqlite-vec DB left open at `process.exit()` could abort `mutex lock failed` (134). `Set<WeakRef>` + `FinalizationRegistry` (leak-free — stress-verified GC pruned 2000/2000), `.close()` deregisters, `closeAllDatabases()` wired into the exit handler. **`npm run smoke` exits cleanly.** **SCOPE (honest, per battle-v3):** this closes the **SQLite** half only. The **real-embedder ONNX/transformers.js worker still aborts 134** on `process.exit()` independent of any DB (P14), and a dual-connection-to-one-WAL + forced `process.exit()` can still 134 (P10) — both OPEN below. Also the underlying SQLite 134 was NOT reproducible as a negative control on Node 22.19.0/darwin, so the SQLite half is defense-in-depth on this stack.
- **fix(logger): recursive secret redaction** (P8) — `redact()` matched only top-level keys and missed `access_token`/`refresh_token`/`client_secret`/`private_key`/`session_token`/`bearer`; nested/array secrets logged cleartext and a circular input threw at `JSON.stringify`. Now recursive over objects+arrays with a WeakSet circular guard + `MAX_DEPTH=6`, own-enumerable keys only (no `__proto__`). Mirrors `sanitizeDeep`. (Residual by design: an unexpected key name like `pwd` is not masked; level env var is `MCP_LOG_LEVEL`.)

A **14-persona battle wave v3** then drove the REAL compiled handlers at this HEAD (a brand-new solo user + a multi-dev team + 12 skeptics, real models, /tmp throwaway, adversarial refute-verify + completeness critic): **all 5 fixes above HOLD**; newcomer succeeded from the skill alone; team primitives + the `agent_id`/`access_level` round-trip (9ffbc9d) + F1/F1b write-isolation all verified end-to-end (incl. P10's real `buildApp`+`POST /mcp` server path). Zero blockers/majors. It also surfaced the P11 residual + two more code bugs (P9, P14) — all now FIXED:
- **fix(repository): `BEGIN IMMEDIATE` on read-then-write txns** (P9) — `db.transaction()`'s deferred `BEGIN` takes the write lock lazily as an UPGRADE, and SQLite ignores `busy_timeout` on an upgrade, so `updateMemory`/`deleteMemory`/`deleteMemoriesByFilter` threw `SQLITE_BUSY` in ~0ms under a concurrent writer and `consolidate` silently under-pruned. The three now run via `.immediate()` (lock at BEGIN → busy_timeout applies → wait then succeed). Read-only (`findNearDuplicates`) + write-first (`insertMemory`/`recordAccess`/`applyDecay`) deliberately left deferred. **RESIDUAL — now FIXED (e6a9ed1, P9-residual):** all 9 other read-then-write `db.transaction` sites are `.immediate()` too — `forget` erase, `condense` persist, `import` process, `extract-entities` process, `entity-store` store+update, `vault/sync` insertBatch+insertAll+apply. The P9 author named 5; an audit of all 20 txn sites found 4 more with identical exposure (`extract-entities` + the two `vault/sync` inserts). `store.ts`/`ingest.ts` (write-first), `consolidate` applyDecay (read hoisted out of txn), `migrations` (startup-only, single connection) stay deferred. Nested `.immediate()` txns (deleteMemory/updateMemory) become SAVEPOINTs inside these outer txns, so converting the OUTER txn is the load-bearing fix. Guarded by 2-connection worker-thread contention tests (storeExtractedEntities + handleForget hard) + a timing-free 9-site source tripwire. Independently corroborated by the battle-v3 worktree+adversarial workflow (verdict: approve, same 9 sites).
- **fix(embeddings): dispose hook + natural-drain `verify-web`** (P14) — the real ONNX/transformers.js worker aborts `mutex lock failed` (exit 134) on `process.exit()` (NO DB needed), so every in-process real-embedder script returned 134, masking failures. **Key finding: `dispose()` does NOT make `process.exit()` safe** (the native race fires on any hard exit) — the fix is **natural drain** (`process.exitCode`, no `process.exit()`), matching `verify-nli`/`verify-hooks`; `dispose()`/`disposeEmbedder()` are the graceful-shutdown release hooks (idempotent, unit-tested). `verify-web.mjs` was the lone force-exit holdout, now converted. **RESIDUAL — guard now FIXED (7eadebb, P14-guard):** a vitest static tripwire (`src/__tests__/scripts/no-process-exit-with-real-embedder.test.ts`) forbids unmarked happy-path `process.exit(` in any `scripts/**/*.mjs` that loads the real embedder in-process (refs `TransformersEmbeddingProvider`/`getEmbedder`/`embeddings/transformers`); mock-only (probe-lossless) + child-process spawners (smoke-mcp) excluded. Audited-safe exits opt out with a `mcp-memory:allow-process-exit` comment (verify-nli 76/110, verify-hooks 33/384 — all pre-model or load-failed). The comment stripper is a string-aware char scanner (the adversarial review proved a single block-comment regex let a `//`-embedded `/*` — the path `dist/hooks/*.js` at verify-hooks:5 — swallow lines 5-81 and hide the real exit at line 33; the scanner tracks line/block/string state so a delimiter inside a `//` comment or string can't open a span). **STILL OPEN:** P10's dual-connection-to-one-WAL + `process.exit()` case (injected `createDatabase` + cached `getReadWriteDb`) can still 134 — harness-only (production `runServe` shares one connection).

A **battle-v5 power-user end-to-end sweep** (this session) drove the full surface on real models, /tmp throwaway: deterministic ground truth all GREEN — `bench` (rerank P@1 .813 / MRR .867, unchanged), `sim-solo`, `sim-team` (git-vault concurrency), `verify-nli` (write-gate), `verify-web` (REST + all 5 dashboard routes, `errors:[]`), `verify-hooks` (SessionStart/PostSearch/PreCompact/Stop), plus a new `scripts/battle/verify-coverage.mjs` driving the 7 previously-never-driven tools + the export→wipe→import DR cycle (**23/23**). Then a 12-agent adversarial Workflow (5 power-user probes → refute-verify → completeness critic) on the surfaces scripts miss. Result: **1 confirmed bug, now FIXED; P9 independently re-validated; one open-item closed.**
- **fix(repository): `updateMemory` no-op guard** (d811f3b, battle-v5) — updateMemory snapshotted `memory_versions` + bumped `version` + `updated_at` UNCONDITIONALLY (only the re-embed was change-guarded), so an id-only or identical-content update wrote a phantom version row and bumped every call (power user saw version=12 after 1 real edit); same root cause inflated the `memory_version_restore` path. Fixed: `changedFields` guard → a no-op returns the row unchanged (no snapshot/bump/updated_at/reindex); SET clauses build from changed fields only. TDD `update-noop-version.test.ts`. 1087 green.
- **P9 re-validated under realistic multi-writer load** — the adversarial probe held the WAL write lock ~250ms while driving forget(hard)/import/consolidate/storeExtractedEntities/vault insertBatch: each WAITED (257–355ms) then succeeded, NONE swallowed `database is locked` into `errors[]`/`report.errors`, reads not over-serialized, no deadlock. 9/9. The silent-under-prune mode is gone.
- **CLOSED: `consolidate dry_run` is FAITHFUL** — the long-suspected dry_run under-count did NOT reproduce across vector-colliding near-dups (mock + real), re-embed-after-merge, max_operations cap, expire, forgetting_floor. dry_run mutates nothing and its predicted counts equal the real run's. Drop it from the open list.
- **DR gap characterized (was untested):** `memory_export` JSON is **live + top-level memories only** — child chunks, entities/graph, `entity_aliases`, `memory_versions`, and `core_memory` do NOT survive export→import (top-level content round-trips + re-embeds + recalls fine). For true disaster recovery, copy the SQLite file; JSON export is a portability/migration format, not a full backup.
- **NEW (doc): entity extractor misses `PostgreSQL` and `Stripe`** — the `postgres` keyword fails the word-boundary match and `Stripe` isn't in the known-tools list, so `use_graph` silently produces 0 edges (degrades to flat search with no signal) for those very common names. Multi-hop graph recall works correctly once a recognized entity (react/redis/docker/prisma/…) is present.
- Minor/doc findings refuted as non-blocking: sub-ms valid-time overlap window in the NLI supersede (transient as_of double-truth); hard-forget DSAR of a condensed memory returns the condensed summary not the erased original (contradicts its docstring); direct handler/REST callers (not MCP) can persist an unvalidated `scope` string (retrievability trap; MCP enum blocks it).

**Battle-v5 completeness critic — highest-risk untested surface — now DRIVEN in round 2** (`scripts/battle/verify-coverage2.mjs`, 26/26, mock embedder + a 12-agent adversarial Workflow). All of `memory_query` (token-budget/hops/scope-isolation), `memory_condense`+restore, `memory_canvas` path-confinement, `vault_sync`/`vault_search`/`vault_status` (handler-level), `memory_list`/`memory_manifest` (pagination/sort/as_of + list=manifest=stats agreement), `memory_attribution`, `memory_unlinked_mentions`, `memory_version_diff`, `memory_version_restore` (incl. the no-op regression) now driven. Adversarial round 2 found **2 real bugs** (critic overall: minor-gaps, all 41 tools driven/audited):
- **FIXED (65cfcf0) fix(vault): `confineToVault` symlink-leaf escape (HIGH/security)** — the guard realpath'd only the deepest EXISTING ancestor, so a DANGLING symlink leaf (`<vault>/x.canvas → /outside`, target absent) was walked past (`existsSync` follows → false) and `writeFileSync` then followed it → wrote OUTSIDE the vault. Hit `memory_canvas` + `memory_export_vault` (shared guard), any name incl. default. Fix: `lstat` the target leaf — reject if it's a symlink (dangling or live). A live symlink-to-existing was already caught by the ancestor realpath; only the dangling leaf slipped. TDD in writer-hardening.test.ts. Residual: a lstat↔write TOCTOU (O_NOFOLLOW would fully close it) — low for a local single-user server.
- **FIXED (3d9e690) fix(vault): soft-tombstone on delete + reject duplicate-id files (HIGH; user decisions applied):** (a) deleting a vault `.md` + re-syncing now **soft-tombstones** (invalidateMemory on parent + child chunks → `valid_to` stamped) instead of hard-deleting — the memory leaves default recall but the row + `memory_versions` survive and `memory_restore` reinstates it; the FK cascade never fires (memory invalidated, not deleted). The changed-file reconcile keeps its delete+reinsert. (b) `syncVault` tracks frontmatter-id ownership (seeded from existing sync-meta, claimed per run); a file whose id is already owned by a **different** file is recorded in `errors[]` and **skipped** before any memory is touched (first file keeps the id; re-sync stable; no fork). TDD `sync-delete-dup.test.ts`. 1095 green.
- Minor/refuted: `memory_query` context can exceed `max_tokens` by a bounded margin (~+21% at a tiny budget=100, ~+1-3% typical — a single un-splittable block + the truncation hint sit outside budget accounting); `queryGraph` does no input clamping (negative `seed_limit` → `slice(0,-n)`) but the Zod boundary rejects it for real MCP callers (defense-in-depth gap); `handleList` accepts a malformed `as_of` (returns all live) but `z.datetime()` rejects it at the MCP boundary. ~8 tools still never driven BY NAME (handleForget/Delete/Graph/History/Versions/SessionNote/ExtractEntities/CoreMemoryReplace) but all have unit tests + behave correctly through the Zod-guarded boundary.

A **battle-v6 solo+team production-readiness sweep** (web session, fresh clone, real models, /tmp + .battle throwaway) ran the full harness green — build, 1317 backend + 8 web tests, smoke (49 tools), bench (rerank P@1 .813 / MRR .867, unchanged), sim-solo, verify-nli/hooks/coverage(23)/coverage2(26)/web (all 5 dashboard routes render with real data after `build:web`), scale (10K search p95 18ms no-rerank / 308ms rerank, sub-second) — and produced **1 real product fix + 1 test de-flake + 1 new gate**:
- **fix(vault): the team git-vault was broken on EVERY concurrent push** (sim-team made a real gate) — `.memory/manifest.json` (the M2 integrity sidecar) was committed as a normal file, but its single-file merkle root + `generated_at` are recomputed over the LOCAL set on every export/sync, so two teammates' manifests can never auto-merge → unresolvable text conflict on the root hash → `git merge` aborts → the post-merge rebuild hook never fires. (The conflict markers also corrupted the manifest JSON, silently disabling `assertVaultIntegrity` via `loadManifestSidecar`→null — which is the ONLY reason the old sim "passed"; the driver's stderr `Merged …` shadowed git's stdout CONFLICT line.) Fix: `vaultGitignore()` now excludes `.memory/manifest.json` (git is the shared tamper-evidence for a git vault; the manifest still guards local + non-git Dropbox/iCloud vaults), and `rebuildHook()` drops the stale local manifest before rebuild (a git-driven merge/checkout is a legitimate change, not tampering, so the guard would otherwise refuse). The graph.json sidecar stays committed — it union-merges. sim-team now writes the REAL scaffolding, captures BOTH git streams, and GATES on the collaboration invariants (was always exit 0).
- **test(redact): de-flaked the two ReDoS linearity guards** — absolute 500/1000ms wall-clock ceilings (Apple-Silicon-calibrated) tripped at ~530ms on a slower shared cloud vCPU for genuinely-linear redaction of a 1M-char / 560KB pathological input. Raised to a generous 4000ms that still catches any re-introduced O(n²) blowup (seconds-to-minutes) without CPU-contention flake.
- **test(battle): added `verify-stress.mjs`** — drove the SQLite path under (1) a 120-wide in-process `Promise.all` burst (120/120 ADDs, 0 rejections), (2) 3 separate OS processes hammering one file (true cross-process WAL contention: 0 SQLITE_BUSY surfaced, 0 errors, **210/210 acknowledged ADDs durable — no lost writes**), and (3) crash-restart (all rows persist + semantic recall works on cold reopen). **Characterized (not a bug): `recordConflicts` auto-retires a near-duplicate OLDER row (superseded_at/valid_to) on store INDEPENDENT of `on_conflict`** — the store.ts "default 'add' yields only NOOP or ADD" comment is about the WRITE DECISION, not conflict recording; a user storing many structurally-similar facts will see fewer LIVE than stored (retired ≠ lost, all rows persist).

**Battle-v6 follow-up — "production SERVICE" gaps closed** (web session): the two surfaces the prior waves left unproven for a hardened multi-client deployment are now driven end-to-end:
- **Authed REST under load (`verify-load.mjs`)** — boots `node dist/index.js serve` (the production entrypoint) with `MCP_AUTH_TOKEN` + rate limiting on, over real HTTP. Proven: bearer auth matrix (401 no/wrong, 200 correct; /health + /live exempt; /metrics bearer-gated), rate limiting (80-burst on a cap-20 bucket → 20×200 + 60×429, every 429 has `Retry-After`, recovers after refill), 300-way concurrent authed list + 80 embedding searches with ZERO 5xx and server healthy after, and malformed→400 / >256kb→413 envelopes. (Single-process p95 climbs under 300-way concurrency — better-sqlite3 is synchronous so queries serialize on one thread — but correctness holds; the rate-limit docs already point to a shared limiter for multi-instance.)
- **Docker deploy path** — `docker build` + `docker run` of the committed Dockerfile verified: multi-stage image builds, container runs as **non-root uid 1000**, /health 200 (db_ok, schema v11), bearer auth enforced, real seeded data served, embedder warms at runtime, semantic search returns correct hits, and the Docker `HEALTHCHECK` reaches `healthy`. **Also fixed:** `.dockerignore` had a bare `node_modules` (top-level only), so `web/node_modules` (~325MB) shipped in the context AND `COPY web/ ./web/` clobbered the image's freshly-installed web deps — added `**/node_modules`/`**/dist` + artifact excludes. (NOTE for this sandbox: the build needs three environment workarounds that are NOT project defects — pull `node:20-slim` via `mirror.gcr.io` to dodge Docker Hub's anon rate limit; inject the egress-proxy CA bundle so npm/apt fetch through the TLS-intercepting proxy; add `python3 make g++` so better-sqlite3 compiles from source because its prebuilt-binary CDN is unreachable through the proxy. On a normal network the unmodified Dockerfile builds as-is on prebuilt binaries.)

**Battle-v6 follow-up #2 — FULL tool surface in the UI (TDD + E2E):** the dashboard previously surfaced only ~9 read/inspect tools over typed REST. A generic **Tools console** (`web/src/pages/Tools.tsx`) now reaches all 49 by driving the server's existing bearer-authed `POST /mcp` endpoint — so NO per-tool REST route was added and the 100%-covered backend is untouched. Built test-first in layers: `web/src/api/mcp.ts` (MCP Streamable-HTTP client — initialize→initialized→tools/list/tools/call, SSE-frame parse, session cache + transparent re-init/retry, bearer auth; 11 tests), `web/src/lib/schema-form.ts` (JSON-Schema→typed form fields + value coercion; 7 tests), `Tools.tsx` (lists tools grouped read/write/destructive/integration, dynamic form per tool, destructive-confirm; 8 tests). Web suite 34 tests green; `tsc -b` + vite build clean. **E2E (`scripts/battle/verify-ui-e2e.mjs`, Playwright):** boots the REAL authed server, primes the token in localStorage, and drives the console through `memory_stats` (read) → `memory_store` (create, id captured, +1 confirmed independently via REST `/api/stats`) → `memory_search` (finds the sentinel) → `memory_delete` (confirm dialog accepted) → back to 0, with ZERO browser console errors — 9/9. **Two real flake fixes during E2E bring-up (not product bugs):** the served single-threaded server blocks `/health` + tool responses during the one-time model load, so the E2E pre-warms the embedder via `/ready`; and `waitUntil:'networkidle'` never settles against MCP's in-flight SSE streams, so it waits on `domcontentloaded` + an explicit tool-button selector. (Note: the bespoke pages still default REST `/api/search` rerank OFF; the console's `memory_search` goes through MCP with rerank ON — the two search precisions differ, as documented.)

Genuinely **open** (verified by the persona pass unless noted):
- **`memory_restore` un-tombstones an NLI-contradiction-retired fact** (P1) — gotchas.md said restore skips a contradiction-retired fact via `superseded_at`, but the NLI `on_conflict='supersede'` path retires via `valid_to` only (`superseded_at` stays NULL, set only by the Jaccard 0.75–0.85 band), so `memory_restore` DOES reinstate it, re-introducing the stale fact next to its correction. Doc corrected; the behavior (whether restore should refuse a contradiction-retired fact) is an open design call.
- **Tool-catalog coverage gap** — the original 7 (`memory_reflect`/`extract_learnings`/`communities`/`tiers`/`questions`/`template`/`related`) + the DR cycle are now DRIVEN (battle-v5 `verify-coverage.mjs`, 23/23). The real-embedder HTTP tenancy sweep is partially covered (`verify-web` drove the REST surface; `POST /mcp` server-dispatch tenancy was P10's mock-embedder path). Remaining never-driven-by-name (battle-v5 critic, ~14 tools): `memory_query` graph traversal, `memory_condense`, `memory_canvas` path confinement, `vault_*` via handlers, `memory_list`/`memory_manifest` pagination+as_of, `memory_attribution`/`memory_unlinked_mentions`/`memory_version_diff`.
- **`memory_query` does not alias-expand** — its seed `hybridSearch` runs with `use_graph` off (original design), so alias resolution doesn't reach it. Enabling `use_graph` there would wire it in but changes seed ranking/latency for every call — a deliberate separate decision, not done.
- **`memory_import` is NOT namespace-forced** (F1b follow-up) — unlike the other write tools (closed in the fix wave above), `MemoryImportSchema` has no top-level namespace (it's per-item under `data[]`), so a `withForcedNs` wrap is a no-op. Forcing imported items on a namespace-forced deployment needs a per-item remap-or-reject decision — deliberately deferred.
- **`memory_consolidate` `dry_run` *may* under-count merges** — apply mutates the index mid-pass, so a faithful preview would simulate against a copy. But a real-embedder persona could not reproduce a divergence in 18 runs (concatenation-then-re-embed rarely pulls a new neighbor over threshold) — treat as a possible edge case, lower priority than once thought.
- **O(n) scale hotspots:** brute-force sqlite-vec KNN on every store (×2) and search. Matters only at low-millions; ANN/HNSW is the fix.
- **Timestamp-collation residual:** rows written space-format before the ISO-Z fixes stay vulnerable until re-edited; no normalizing migration exists.
- **Minors:** `session_note`/`core_memory_append` unbounded growth; no `.max()` on search query; `init --project` ≠ `--scope project`; `legal` chunker not clause-aware; `vault_sync` doesn't strip a leading H1; content round-trip gains a trailing newline.
- **REST `/api/search` rerank off by default** → dashboard lower precision than MCP; a REST opt-in param is the candidate fix.
- **No top-level MCP server `instructions` field** — `createServer()` sets only name+version. Adding `instructions` (+ tightening the 41 tool descriptions) would help **every** MCP client. Highest-leverage "how to use this" investment.
- **Structure debt:** `src/schemas/index.ts` + `src/db/repository.ts` splits deferred (churns 100+ test imports).

## 5. Competitive positioning

vs mem0 / Zep / Letta / Cognee / Supermemory — what this server does that they don't:
- **100% local-first, single SQLite file, zero telemetry, $0/token** — no cloud rival guarantees data never leaves the machine. The core moat.
- **Hybrid vector + FTS5/BM25 RRF in one local engine** — no Neo4j/FalkorDB/Modal stack to run.
- **Obsidian round-trip + JSON Canvas + REST + dashboard in one process.**
- **Enterprise governance** (scopes/departments/access_level/GDPR forget/attribution) beyond native ChatGPT/Claude memory.
- **Cross-model** — any MCP client.

Honest weaknesses: weaker 384-dim MiniLM base (mid-pack precision; rerank closes some gap), O(n) scale ceiling (~low-millions), heuristic regex extraction (lower recall than LLM extraction), small published gold set (24 rows / 16 queries — credibility caveat). The pitch is **$0/token + 0% cloud exposure**, not top-of-leaderboard accuracy.

## 6. Doc drift to fix (when touching docs)

- **README** says "37 tools" (and "17" in older sections) — actual is **41**. The verify line "17 tools" is stale.
- **`docs/ENV.md`** omits env vars the code reads: `MCP_API_NAMESPACE`, `MCP_TRUSTED_IP_HEADER`, `MCP_VAULT_PATH`, `MCP_VAULT_WRITE_THROUGH`, `MCP_PUBLISH_RATELIMIT_CAPACITY`/`_REFILL_PER_SEC`.
- **`docs/openapi.yaml`** does not define the `/publish` wiki routes (they exist in `src/api/routes.ts` + `src/publish/wiki.ts`).
- **`CONTRIBUTING.md`** cites coverage thresholds 75/75/70; actual enforced is 100/100/99/90.
- **`docs/RUNBOOK.md`** shows `schema_version|4` as expected `schema_meta` output (~line 122) — stale; `CURRENT_SCHEMA_VERSION=9`.
- **Config dead key:** `storage.db_path` — `db/connection.ts` reads `MCP_MEMORY_DB_PATH` env, not the config key.
- Web pins Vitest `^4`, backend `^3` (version skew).
